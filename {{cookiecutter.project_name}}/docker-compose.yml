version: '3.7'
services:
  kedro:
    container_name: {{cookiecutter.project_name}}
    build:
      context: .
      dockerfile: ./cpu.dockerfile
    ports:
      - "${PORT:-2222}:8080"
    env_file:
      - .env
    volumes:
      - .:/app/
